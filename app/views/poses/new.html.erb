 <h3>Build sequence: <strong><%= @sequence.name.upcase %></strong></h3>
<div class="row">

	<div class="col-md-5">
		<%= simple_form_for [@sequence, @pose] do |form| %>
			<%= form.input :name, :label => "Pose", :collection => @poses.first(5) %>
			<%= form.submit 'Add pose' %>
		<% end %>

	</div>
</div>